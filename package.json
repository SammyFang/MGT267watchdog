{
  "name": "mgt267-watchdog",
  "version": "1.0.0",
  "private": true,
  "description": "Watch Supply Chain Game warehouse inventory, cash, and day, then send email alerts.",
  "type": "commonjs",
  "scripts": {
    "monitor": "node src/monitor.js --once",
    "test-email": "node src/monitor.js --test-email",
    "warning-email": "node src/monitor.js --warning-email",
    "watch": "node src/monitor.js --watch"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "nodemailer": "^8.0.7"
  },
  "engines": {
    "node": ">=20"
  }
}
